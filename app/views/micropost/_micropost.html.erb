<% current_user.microposts.all.each do |m| %>
	<div class="list-group">
	  <a href="#" class="list-group-item active">
	    <h4 class="list-group-item-heading"><%= m.content %></h4>
	    <p class="list-group-item-text">Hace <%= time_ago_in_words(m.created_at) %></p>
	  </a>
	</div>
<% end %>
