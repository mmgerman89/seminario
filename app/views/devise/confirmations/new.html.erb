
<div class="panel panel-default">
  <div class="panel-heading">
    <h4><%= t('.resend_confirmation_instructions', :default => 'Volver a enviar instrucciones de confirmación') %></h4>
  </div>
  <div class="panel-body">
    <%= form_for(resource, :as => resource_name, :url => confirmation_path(resource_name), :html => { :method => :post, role: "form" }) do |f| %>
      <div class="form-group">
        <%= f.label "E-mail" %>
        <%= f.email_field :email, class: "form-control"  %>
      </div>

      <%= f.submit t('.resend_confirmation_instructions', :default => 'Enviar'), class: "btn btn-primary" %>
    <% end %>
  </div>
</div>

<%= render "devise/shared/links" %>o