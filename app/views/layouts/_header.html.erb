<header class="navbar navbar-default">
	<div class="container-fluid">
		<!-- Brand and toggle get grouped for better mobile display -->
		<div class="navbar-header">
			<a class="navbar-brand" href="<%= root_path %>">RestOnLine</a>
		</div> <!-- navbar-header -->
		
		<!-- Collect the nav links, forms, and other content for toggling -->
		<div class="collapse navbar-collapse", id="bs-example-navbar-collapse-1">
			<ul class="nav navbar-nav">
				<%= nav_link "Inicio", 		root_path %>
				<%= nav_link "Comercios", 	commerces_path %>
				<%= nav_link "Ayuda", 		help_path %>
				<%= nav_link "Acerca de", 	about_path %>
				<%= nav_link "Contacto", 	contact_path %>
			</ul>
			<ul class="nav navbar-nav navbar-right">
				<% if user_signed_in? %>
					<li><%= link_to "#{current_user.email}" %></li>
					<li class="dropdown">
						<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Cuenta <span class="caret"></span></a>
						<ul class="dropdown-menu" role="menu">
							<li><%= link_to "Editar cuenta", edit_user_registration_path %></li>
							<li><a href="#">Privacidad</a></li>
							<li class="divider"></li>
							<li><%= link_to "Mis comercios", my_commerces_path %></li>
							<li class="divider"></li>
							<li><%= link_to "Salir", destroy_user_session_path, method: :delete %></li>
						</ul>
					</li>
				<%else%>
					<%= nav_link "Iniciar Sesión", new_user_session_path %>
					<%= nav_link "Registrarme", new_user_registration_path %>
				<%end%>
			</ul>
		</div>
	</div>
</header>

