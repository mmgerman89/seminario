<%
	@tabs = Hash.new
	@tabs["menus"] = "active"
%>
<%= render layout: 'layouts/tabs_profile_branch', locals: {title: 'Carta'} {  } %>
<h1 class="cart-text"><%= render 'layouts/cart_text' if user_signed_in? %></h1>
<div class="menu">
	<div class="row">
		<% @menu.each do |item| %>
			<%= render layout: 'shared/item_branch', locals: {item: item} {} %>
		<% end %>
	</div>
</div>

